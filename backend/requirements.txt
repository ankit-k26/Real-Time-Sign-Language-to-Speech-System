# Sign Language to Speech System — Backend Dependencies
# Install with:  pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Core CV + ML
# opencv-python-headless: no GUI deps — required for Render/Linux containers
opencv-python-headless>=4.9.0
mediapipe>=0.10.32
numpy>=1.26.0,<2.0
tensorflow>=2.18.0
# tf-keras: Keras 2 compatibility shim for TF 2.16+
# Required because sign_model.keras was trained with Keras 2 (batch_input_shape / time_major)
# Combined with TF_USE_LEGACY_KERAS=1 env var this avoids retraining.
tf-keras>=2.18.0
scikit-learn>=1.4.0

# LLM integration
google-genai>=1.0.0

# Text-to-Speech
# pyttsx3 removed — requires local audio driver (not available on Render Linux)
gTTS>=2.5.0

# Optional: richer emotion detection
# deepface>=0.0.93
